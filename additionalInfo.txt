~ CSS optimization - https://matthewjamestaylor.com/add-css-to-html
--> Each CSS file requires it's own HTTP request
--> Embedded CSS rules are render blocking

- Add eslinting - does it make sense to do this for this project ?
- Add watching for changes (so you don't need to keep refreshing the browser window to view changes)